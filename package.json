{
  "name": "uploadthing-mcp",
  "version": "1.0.0",
  "description": "MCP server for UploadThing, deployable as a Cloudflare Worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "uploadthing": "^7.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240924.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.78.0"
  }
}
